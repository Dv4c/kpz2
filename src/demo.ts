import { add, capitalize } from './index.js';

// ПОМИЛКА ЛІНТИНГУ навмисно: подвійні лапки

console.log('sum(any):', add(2, 3));

// рядок зі змінною unused видаліть

console.log('capitalize(any):', capitalize('hello'));
